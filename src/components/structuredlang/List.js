import React from 'react';
import PropTypes from 'prop-types';

const List = ({ elements }) => (
    <span>List: not yet implemented</span>
)

List.propTypes = {
    elements: PropTypes.array.isRequired,
};

export default List;
